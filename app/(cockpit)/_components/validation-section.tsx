export function ValidationSection({ label }: { label: string }) {
  return (
    <p className="mb-2 mt-[18px] text-[11px] font-semibold uppercase tracking-[.08em] text-faint">
      {label}
    </p>
  );
}
