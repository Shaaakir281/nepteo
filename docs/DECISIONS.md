# Décisions

## Ouvertes (à trancher avant l'outbound et la bêta multi-client)

| # | Décision | Enjeu | Statut |
|---|----------|-------|--------|
| 1 | **Client pilote** | Critique — aucune preuve de valeur terrain sans données et usage réels | À trancher |
| 4 | Niveau d'autonomie par défaut au lancement | Confiance client vs valeur démontrée | À trancher |
| 5 | Fournisseur d'envoi UE et identité d'expéditeur | Prérequis C7, délivrabilité et conformité | À trancher par Fathi |

## Actées

Le 14 août 2026, tant que l'application Meta reste en mode développement,
Nepteo intercale un parcours pilote honnête avant OAuth. Une demande porte
l'organisation, l'utilisateur, l'e-mail Facebook et un lien de profil
facultatif ; elle est isolée par RLS et suit `requested → ready → connected`.
L'ajout du testeur et sa notification restent manuels. Aucun mot de passe,
jeton, App ID ou secret n'est demandé au client. Cette décision évite de
prétendre automatiser une capacité Meta indisponible tout en permettant de
reprendre OAuth après validation.

| Date | Décision | Raison |
|------|----------|--------|
| 2026-08-13 | **La configuration OAuth Meta de Nepteo est une configuration plateforme unique, jamais un secret demandé à chaque client.** Les clients passent par le bouton Connecter, consentent seulement à `ads_read`, choisissent leur compte et peuvent révoquer l'accès. Le déploiement doit fournir ensemble `META_OAUTH_APP_ID`, `META_OAUTH_APP_SECRET` par `secretref:` et `META_GRAPH_API_VERSION`; une configuration partielle échoue avant mutation Azure. | La recette administrateur a prouvé le callback public, le compte `Fathi Mts`, EUR, Europe/Paris et une lecture vide sans `ads_management`. Centraliser le secret applicatif dans Azure/GitHub évite toute transmission client et empêche qu'un déploiement ultérieur efface la configuration manuelle. Cette preuve ne ferme pas G1 sans smoke atomique/JWT et échantillon campagne/dépense/résultat non vide. |
| 2026-08-12 | **META-METRICS applique une photographie Meta complète et atomique, sans convertir les actions Meta en conversions aval.** L'identité quotidienne est organisation + fournisseur + compte + campagne + date ; les résultats sont enfants de cette ligne et ajoutent type + attribution. Dépense/impressions/clics et résultats portent la provenance `provider_reported`, tandis que `conversions`, `revenue` et `outcome_provenance` restent `NULL`. Un échec de page, borne, timeout, validation ou persistance ne remplace jamais la dernière photographie complète et produit au plus un run/journal idempotent. | Une absence ne doit jamais devenir zéro et une action déclarée par Meta ne prouve ni acquisition, ni revenu, ni CAC, ni ROAS. Le remplacement transactionnel rend suppressions et changements réconciliables ; les RPC restent service-role, les lectures `ads_read` et l'interface n'expose aucune mutation. La migration locale `0029` doit encore passer staging/JWT et un échantillon Meta autorisé avant G1. |
| 2026-08-12 | **La boucle Campagnes mesurable devient la priorité produit immédiate.** L'ordre est désormais : contrat de vérité des indicateurs → `META-METRICS` vers `ad_metrics` → cockpit « Budget et résultats » → recommandations prudentes entre campagnes Meta → mesure avant/après → porte distincte d'aptitude à muter → pause Meta supervisée ; Google Ads en lecture vient ensuite, avant tout arbitrage Meta ↔ Google. Cette décision remplace les ordres antérieurs qui faisaient de Salesforce, d'un nouveau catalogue de connecteurs, de la pause ou du lancement Meta la prochaine étape. | L'audit du produit montre que le moteur CAMP-1/2 existe, tandis que META-READ s'arrête dans `connectors.config`, que le budget prévu n'est pas rapproché d'une campagne fournisseur et que `ad_metrics` ne prouve ni devise, attribution, conversions aval ni revenu. La valeur à fermer est donc `données réelles → budget/résultats → recommandation → décision → mesure`. Aucun ROAS réel n'est affiché sans revenu rapproché et vérifiable ; résultats Meta et conversions/revenus aval restent distincts ; la preuve lecture/analyse/mesure ne confère aucun droit d'écriture. Salesforce, LinkedIn, lancement payant, écritures fournisseur, automatisation avancée et intégration Andromeda directe sont explicitement dépriorisés. |
| 2026-08-12 | **La release sécurité déployée dans `nepteo-prod--0000030` durcit les dépendances, les trois OAuth et les réponses HTTP sans ouvrir de capacité fournisseur.** Les URLs publiques OAuth proviennent de `APP_URL`; chaque état est signé et lié au navigateur, au fournisseur, à l'utilisateur et à l'organisation ; les cookies sont courts, `HttpOnly`, `SameSite=Lax` et `Secure` en production. Des en-têtes CSP/HSTS/anti-iframe/`nosniff`/référent/permissions sont servis globalement. | L'audit relevait des avis npm, l'utilisation de l'origine interne de la requête dans les OAuth et l'absence d'en-têtes de défense en profondeur en production. PR #43, CI de PR et de `main`, déploiement protégé et contrôles HTTP publics sont verts. Aucune migration, permission Ads ou écriture externe n'a été ajoutée ; les recettes OAuth réelles restent distinctes. |
| 2026-08-12 | **Andromeda n'est pas une IA que Nepteo prompte directement.** Nepteo prépare, justifie et fait valider des objets de campagne structurés ; une future capacité `ads_management`, séparément autorisée, les transmettra via l'API Marketing. Meta Advantage+/Andromeda restera responsable de la sélection et de la diffusion. Toute Conversions API sera un lot distinct, fondé sur des événements consentis, minimisés et dédupliqués. | Andromeda est le moteur interne de récupération publicitaire de Meta, sans interface de prompt métier exposée à Nepteo. Cette séparation conserve une responsabilité claire : Nepteo maîtrise les entrées, les validations et la traçabilité ; Meta optimise la diffusion dans sa plateforme. |
| 2026-08-10 | **La PR #33 est la release applicative courante dans `nepteo-prod--0000024`. Derrière un proxy, toutes les redirections absolues du callback Auth sont construites depuis `APP_URL`, jamais depuis `request.url`; une origine publique invalide échoue fermée avant consommation du jeton.** Les flux `code` et `token_hash` sont conservés ; le schéma reste à 28 et les connecteurs sont inchangés. | Un nouvel email français a prouvé que Site URL, Redirect URL, modèle et SMTP corrects ne suffisaient pas : la route post-callback exposait l'origine interne Azure `0.0.0.0:3000`. Quatre variantes négatives renvoient désormais 307 vers le domaine public. Le clic positif d'un email neuf et le logout mobile authentifié restent des preuves distinctes à jouer. |
| 2026-08-10 | **Historique — la PR #31 a livré dans `nepteo-prod--0000023` la déconnexion mobile, Afficher/Masquer le mot de passe et la source versionnée du modèle français.** La publication de ce modèle dans Supabase est restée une opération de configuration hébergée distincte. Le schéma est resté à 28, sans modification des connecteurs. | La première hypothèse « ancien email uniquement » a ensuite été invalidée par un nouvel email français. La PR #33 ci-dessus corrige la seconde cause, applicative, sans remettre en cause les simplifications UX de PR #31. |
| 2026-08-09 | **La PR #29 a établi la release design allégé, parcours campagne-first et génération Story sur le schéma 28 dans `nepteo-prod--0000022`, sans écriture fournisseur ; cette release est désormais historique.** Une unique génération payante a été autorisée sur une organisation E2E dédiée, puis sélectionnée et validée avec la campagne. L'objet, l'asset, la requête, l'action/campagne et l'acteur du run ont été supprimés ; l'organisation-coquille et le journal append-only sont conservés. L'acteur CSV dédié a été reprovisionné et son smoke officiel est de nouveau vert. `0028` ayant déjà été appliquée et confirmée par Fathi avant cette séquence, la règle de staging ci-dessous devient prospective pour `0029+`. | La CI, le déploiement et le parcours principal sont prouvés sans utiliser de données client ni déclencher de lancement, d'outbox ou de mutation Ads. Cette preuve ne ferme pas les gates de concurrence multi-session, d'isolation inter-tenant, de panne Storage/pending, d'OAuth Meta réel ou de valeur commanditaire. |
| 2026-08-09 | **Règle prospective à partir de `0029` : toute nouvelle migration passe d'abord par une base Supabase de staging/recette distincte.** RLS/JWT, quotas et concurrence, Storage, réconciliation et transactions métier doivent y être prouvés avant production ; application et promotion exigent ensuite une autorisation explicite. Cette règle ne prétend pas rétroactivement que `0028`, déjà appliquée lorsqu'elle a été formulée, a suivi ce chemin. | Les tests Node du lot créatif vérifient surtout des contrats statiques, pas l'exécution PostgreSQL, les politiques avec de vrais JWT, les courses intersessions ni les effets Storage. Séparer la première migration de la production rend les échecs récupérables et maintient distincts code fusionné, schéma migré, application déployée et recette réussie. |
| 2026-08-09 | **La Story est une proposition rattachée à une campagne, jamais un écran créatif vide par défaut.** Le workspace sélectionne une campagne récente, reprend son message et recommande le format selon le canal ; le mode libre reste secondaire. La génération reste un geste explicite et le visuel est versionné. Si une version est retenue avant l'approbation, campagne et visuel sont validés atomiquement ; si la campagne est approuvée sans visuel, une version peut encore être créée puis validée explicitement dans le studio. Aucun asset n'est publié automatiquement chez un fournisseur. | Le retour de Fathi confirme que la création de Story appartient au parcours Campagne. Partir du contexte déjà saisi réduit la densité et les répétitions, évite de redemander un brief, garde la validation humaine et permet d'ajouter l'API image sans ouvrir une capacité d'écriture Ads. |
| 2026-08-08 | **Toute nouvelle tâche finit et commence avec des éléments directement actionnables.** La fiche de clôture se termine par un bloc unique `Commande à copier-coller`. À l'ouverture, Codex annonce le modèle et l'état source attendu, puis vérifie chemin, HEAD et fichiers sentinelles dans le premier diagnostic. Sans choix explicite de Fathi, aucun modèle n'est imposé par Codex : l'application utilise son modèle par défaut. | La première tentative REL-0 a été créée depuis le checkout enregistré `C:\dev\agent_marketing` au lieu du worktree actif `5429` ; elle a donc conclu à tort que CAMP-0/1/2 étaient absents. Le worktree source restait intact à `e023984`, readiness 27 et migrations `0025–0027`. Cette vérification empêche qu'un mauvais checkout soit présenté comme un blocage utilisateur. |
| 2026-08-08 | **Le travail à distance suit un protocole mobile et une tâche Codex par micro-lot.** Chaque fin de lot confirme l'état exact, résume les changements et contrôles, fournit une recette téléphone de cinq minutes maximum, propose le lot suivant puis attend son approbation. `GO <lot>` ouvre et lance seulement le travail local dans une nouvelle tâche ; `PUBLIER <lot>` autorise uniquement les opérations externes préalablement énumérées ; `VERT <lot> + GO <suivant>` consigne le verdict et permet d'enchaîner. | Fathi testera principalement depuis son téléphone pendant ses déplacements. Séparer lancement local, publication et verdict évite qu'une réponse courte étende involontairement l'autorisation, tout en permettant d'ouvrir la tâche suivante rapidement. Un lot non testable utilement sur téléphone conserve une preuve technique honnête au lieu d'inventer une recette. |
| 2026-08-08 | **CONN-0 préserve intégralement le catalogue existant : cinq catégories et vingt-deux propositions.** HubSpot, Pipedrive, Salesforce, Airtable, Fichier CSV, Google Sheets, Notion ; Google Analytics, Matomo, PostHog, Site internet ; Meta Ads, Google Ads, LinkedIn Ads ; Brevo, Mailchimp, ActiveCampaign, Gmail ; Stripe, Shopify, WooCommerce et Logiciel de facturation restent visibles et catégorisés comme aujourd'hui. Les propositions de la maquette Growth Cockpit sont ajoutées sans remplacement. | Fathi veut conserver l'étendue visible du produit et son classement métier. « Catalogue honnête » qualifie uniquement les états et les actions : un bouton `Connecter` suppose un parcours réel ; sinon la carte indique honnêtement `Demander`, `Configurer`, `Prévu` ou l'état vérifié. Cette correction n'autorise ni connexion ni mutation fournisseur. |
| 2026-08-08 | **Les intégrateurs avancent désormais par lots regroupés vérifiables.** CONN-1 couvre ensemble consentement, lecture vérifiée, erreur, pause et révocation sur les parcours existants ; META-READ, REV-READ, OUTBOUND-READ et AUTOMATION-LATE regroupent ensuite les étapes indissociables de chaque famille. | Fathi ne veut plus de micro-lots artificiels. La publication reste contrôlée par une porte complète et une recette mobile, mais un lot peut traiter plusieurs étapes cohérentes sans multiplier les cycles de publication. |
| 2026-08-08 | **La roadmap Campagnes est désormais livrée par micro-lots recettés en ligne un par un, et reprend le catalogue d'intégrateurs de la maquette sans reprendre ses états fictifs.** La release de rattrapage CAMP-0/1/2 est l'unique exception groupée ; après son verdict, CONN-0 expose un catalogue honnête, puis chaque adaptateur suit contrat officiel, connexion, lecture et vérité UI avant toute écriture distinctement autorisée. Meta reste le pilote par défaut ; LinkedIn Ads, HeyReach, Resend, PostHog, Stripe, Google Ads, Supabase interne, TikTok Ads, n8n et MCP personnalisé restent planifiés et conditionnels. | Les badges `API`/`MCP`, URLs, outils, métriques, mentions « connecté », « officiel » ou « temps réel » de `nepteo-growth-cockpit-v2.html` sont illustratifs — Google Ads y est même décrit de façon contradictoire. Une capacité ne devient réelle qu'après vérification de l'identité distante et d'une lecture complète. Les écritures engageantes conservent proposition, validation, journal, idempotence, absence de retry ambigu et autorisation séparée ; MCP personnalisé et n8n arrivent en dernier à cause des risques d'egress, SSRF et outils non fiables. |
| 2026-08-04 | **Le socle CAMP-2 est implémenté localement comme un cockpit de décision reproductible, sans exécution Ads.** Les fenêtres courante et de comparaison sont explicites ; KPI, activité et recommandation proviennent uniquement de `ad_metrics`, `actions` et `journal`. Impressions et clics alimentent CPM/CTR avec dénominateurs et comparaisons explicites. Le résumé relit seulement les contrôles persistés, les connecteurs hors scénario et le dernier démarrage journalisé ; la recherche prospect reste expurgée et bornée. Un rapport 7 jours contre 7 jours et quatre questions fixes sont dérivés sans IA. La récence ne fabrique pas un état fournisseur, tout refus garde son motif, et `0027` crée les propositions `ads_pause_*` avec leur journal de façon atomique et idempotente tout en excluant Ads du claim comme de la finalisation. | Chaque nombre et événement doit être reproductible sans transformer une corrélation CPM/CTR en cause, un démarrage en succès ou un statut connecté en santé temps réel. Une validation Ads signifie seulement « Validée — non appliquée » : CAMP-2 ne crée ni outbox, ni appel fournisseur, ni IA, ni dépense. Fatigue/saturation causales, question libre, heartbeat agent/connecteurs, audit créatif réel, Google/LinkedIn réels, statuts fournisseur, devise/attribution, créas/adsets, A/B/scaling, MRR/churn/LTV/blended et temps réel restent explicitement différés ; le schéma ne permet pas de les simuler. Les migrations et toute publication restent hors de l'autorisation locale. |
| 2026-08-04 | **CAMP-1 est implémenté localement comme un studio de proposition arbitrable, toujours non exécutable.** La preuve historique porte sur 30 jours et n'autorise une projection qu'avec au moins 7 jours distincts, une dépense positive et 10 conversions ; à défaut, aucun benchmark de canal ne remplit le vide. Le studio v2 permet d'éditer adsets, allocation, hooks retenus et formats attendus, tous revalidés ou dérivés côté serveur. La veille concurrentielle reste un geste séparé et confirmé via `runResearch`. `0026` distingue l'intention canonique du snapshot serveur et compare l'intention lors d'un rejeu idempotent. | Une proposition « top » doit rester corrigeable et expliquer l'origine de chaque chiffre sans transformer une donnée rare en certitude. Séparer intention et snapshot évite qu'une évolution des métriques transforme le même double clic en conflit. CAMP-1 n'ajoute aucune exécution, outbox, mutation Ads/email, publication ou permission fournisseur ; les migrations, appels payants et toute publication restent hors de cette autorisation locale. |
| 2026-08-03 | **CAMP-0 est implémenté localement comme une proposition strictement non exécutable.** Aucun choix engageant n'est présélectionné ; brief et variantes sont validés côté serveur ; budget total et plan sont recalculés depuis le brief ; `0025` sépare la clé de soumission de la clé d'exécution et crée atomiquement une action `launch_campaign` `proposed` avec son unique journal. Une validation s'affiche « Validée — non lancée » et ne fournit aucun bouton Exécuter. La génération de variantes reste un geste explicite, un appel au plus, timeout 20 s, `maxRetries: 0` et télémétrie sans prompts/réponses. | CAMP-0 doit prouver la fiabilité de la proposition avant toute permission Ads/email : double clic sans doublon, aucun demi-état, zéro outbox, aucun statut ou effet fournisseur. Ce lot local ne redéfinit ni le kill switch ni l'isolation démo et n'autorise aucune migration, publication ou recette payante. |
| 2026-08-03 | **La première intégration du tutoriel est autorisée sur une branche locale, sans publication implicite.** L’onboarding commence par le choix explicite « scénario d’exemple » / « entreprise réelle » ; le centre `Prise en main` vit hors des cinq entrées métier, ouvre les vrais écrans et conserve seulement une progression versionnée dans le navigateur. Le choix d’un scénario ne le charge pas : une confirmation distincte reste obligatoire. Les anciennes bulles `CoachBubble` sont absorbées par ce centre. | Fathi a constaté en production que l’ancien couloir nom/activité/principes puis analyse web était toujours seul présent. L’incrément rend enfin le parcours visible sans ajouter de migration, télémétrie, appel payant, retry, envoi ou mutation automatique. Les critères terrain G1 ne sont pas déclarés acquis et push, fusion et déploiement exigent un nouvel accord. |
| 2026-08-02 | **Le laboratoire web conserve un compteur quotidien mais aucun plafond bloquant** : `reserve_research_call(..., null)` incrémente toujours sous verrou d'organisation, tandis que la pause, le journal avant appel, le cache, le timeout et l'absence de retry automatique restent inchangés. **La sortie `website_preview` OpenAI utilise `text.format` avec un JSON Schema strict dans le même appel `web_search`**, avec une borne de stockage dédiée de 12 000 caractères ; tout ancien cache marqué `ok` mais inexploitable est reclassé `failed` avant l'unique appel confirmé | Le test réel de `bogasolution.com` a trouvé 6 sources mais la couche applicative a tronqué la réponse à exactement 4 000 caractères, supprimant l'accolade finale puis affichant à tort « aucun résultat structuré ». Le compteur `2/30` et le crédit OpenAI n'étaient pas la cause. Fathi ne souhaite aucune limite budgétaire artificielle ; le suivi atomique est néanmoins conservé pour l'observabilité et la pause d'urgence |
| 2026-07-31 | **Nepteo introduit une prise en main par missions. L’onboarding en constitue le niveau 0 : l’utilisateur choisit explicitement entre découvrir avec une entreprise d’exemple et configurer son entreprise ; chaque information demandée explique son utilité. La progression mesure les gestes découverts, jamais la compétence marketing.** | Le fondateur lui-même a rencontré une difficulté de compréhension malgré sa connaissance du produit. Un guidage par l’action révèle la valeur sans déplacer prématurément les fonctions. La voie exemple/réel reste exclusive et aucun message ou campagne n’est envoyé. |
| 2026-07 | Positionnement entre expert et grand public ; règle vocabulaire (standard gardé, jargon plateforme coupé) | Résolution tension V2 Charly vs mémo |
| 2026-07 | Stack : Next.js + Supabase EU + Vercel AI SDK multi-fournisseurs + OAuth officiels | Simplicité, réversibilité des modèles, RGPD, un seul déploiement |
| 2026-07 | Validation humaine par défaut ; exécution directe seulement si réversible et faible risque | Cœur du produit |
| 2026-07 | Non négociables : idempotence + journal avant envoi, garde-fous serveur, chiffrement tokens | Fiabilité dès le jour 1 |
| 2026-07 | Hébergement : Docker → Azure Container Apps (région EU), CI/CD GitHub Actions, images dans ACR | Azure déjà connecté au GitHub de Fathi ; scale-to-zero économique |
| 2026-07 | Couche IA multi-fournisseurs via Vercel AI SDK : Anthropic par défaut, OpenAI (et autres) pour tests comparatifs ; modèles choisis par env | Comparer qualité/coût sans réécrire le code agent |
| 2026-07 | **Pas de file de jobs en Phase 1** : sync via route sécurisée `/api/cron/sync` (Bearer `CRON_SECRET`) appelée par un scheduler externe (GitHub Actions planifié, Container Apps Job possible). pg-boss réévalué quand retries/volumes l'exigeront | Éviter l'infra prématurée ; un appel HTTP quotidien suffit largement au volume Phase 1 |
| 2026-07 | **Premiers connecteurs : Google Sheets + Notion, en lecture seule** — base de prospects/contacts la plus simple pour démarrer, sans dépendre d'un CRM installé chez le pilote | Choix de Fathi ; barrière d'entrée minimale pour les premières PME testées |
| 2026-07 | Modèles attribués **par tâche produit** (registre `LLM_TASKS` dans lib/llm.ts, override env `LLM_TASK_*`) ; observabilité et évals **Langfuse** prévues avant la Phase 2 (traces par tâche, tests A/B de modèles) | Le bon modèle au bon endroit ; coûts et qualité mesurables tâche par tâche |
| 2026-07 | **Nepteo ne détient ni ne déplace jamais de fonds** : pour les campagnes payantes, le budget vit sur le compte publicitaire du client (Meta/Google), avec son propre moyen de paiement. L'agent ne fait que des appels API (budget, statut, ciblage) ; c'est la plateforme qui facture le client. Aucun encaissement, aucun solde, aucun reversement par Nepteo | Éviter le statut d'établissement de paiement (agréments, DSP2, contrôle des fonds). Conséquence : « lancer une campagne / augmenter un budget » = action la plus engageante → validation obligatoire + plafond de budget serveur ; on démarre par « mettre en pause » (ne fait que réduire la dépense) |
| 2026-07 | **Correspondance de colonnes configurable** à la connexion d'un connecteur, en complément de la détection automatique. L'écran est construit ; il couvre nom, email, entreprise, statut, notes et dernier contact depuis C8 | Les clients arrivent avec leurs propres intitulés. Le schéma `prospects` reste interne et normalisé ; le mapping explicite supprime la rigidité avant l'ouverture multi-clients |
| 2026-07-25 | **Recherche web via Perplexity (Agent API), hors de la couche LLM** : `lib/research/*` appelle `POST /v1/agent` en `fetch` (aucune dépendance npm) pour COLLECTER des faits sourcés ; la couche LLM existante (`LLM_TASKS`) les met en forme. Presets par type de recherche (`low` pour l'identité, `fast` pour une fiche société), surcharge par `PERPLEXITY_PRESET` | Deux rôles distincts : chercher ≠ rédiger. Garder Perplexity hors de `lib/llm.ts` évite de mélanger un fournisseur de recherche facturé à la requête avec le registre de modèles de génération. Sans `PERPLEXITY_API_KEY`, la recherche est simplement désactivée — rien ne casse |
| 2026-07-25 | **L'enrichissement d'un prospect porte sur sa SOCIÉTÉ, jamais sur la personne** : les requêtes interdisent explicitement les informations sur des personnes physiques ; le cache est indexé par société | RGPD : rechercher sur une personne nommée exigerait une base légale documentée, l'information des personnes et un droit d'opposition. Le contexte société (secteur, taille, actualité publique) est ce qui sert réellement à personnaliser une relance, avec une base légale simple. À rouvrir seulement si un pilote le demande, et alors avec le cadre juridique complet |
| 2026-07-25 | **Une recherche web est traitée comme une exécution** : garde-fous serveur (clé > bouton d'arrêt > sujet > plafond `MAX_RESEARCH_PER_DAY`), réservation puis entrée au journal AVANT l'appel, résultat stocké par (org, type, sujet). Seul un résultat `status = ok` peut être réutilisé pendant 30 jours comme cache de réponse | C'est un appel externe **facturé à la requête**. Même discipline que l'envoi : rien de coûteux sans trace préalable et sans plafond non contournable depuis l'UI. Les échecs restent stockés et tracés pour diagnostic, mais ne sont jamais renvoyés comme une réponse en cache |
| 2026-07-26 | **La recherche web devient multi-fournisseurs : OpenAI `web_search` (Responses API) en plus de Perplexity**, derrière une interface unique (`lib/research/provider.ts`, `RESEARCH_PROVIDER`). Perplexity **n'est pas supprimé** ; le fournisseur effectif est choisi par variable d'env, à défaut par la présence d'une clé. `POST /v1/responses` en `fetch` natif, `tools: [{type:"web_search"}]` + `include: ["web_search_call.action.sources"]` — toujours aucune dépendance npm. Chantier cadré dans `docs/projets/recherche-web-openai.md` | **Contexte** : la clé OpenAI est déjà active, le compte Perplexity API reste à créer — donc la recherche est éteinte en pratique, et avec elle le 2e écran d'onboarding (le premier « waouh » avant tout connecteur). **Décision** : ajouter un fournisseur plutôt qu'en troquer un, pour que le choix reste réversible en une variable d'env. La séparation « la recherche vit hors de `lib/llm.ts` » (25/07) est ce qui rend ce changement peu coûteux : seuls un adaptateur et un parseur bougent. **Conséquences** : (1) une requête OpenAI peut déclencher **plusieurs recherches facturées** en mode agentique — `MAX_RESEARCH_PER_DAY` compte des appels `runResearch`, pas des recherches du fournisseur ; à mesurer et borner (`search_context_size`), sinon le plafond ment ; (2) `researchConfigured()` quitte `perplexity.ts` pour `provider.ts` (4 imports à suivre) ; (3) le 2e écran d'onboarding se rallume dès qu'une clé existe — à retester avant la démo ; (4) `gpt-4o-search-preview` est arrêté depuis le 2026-07-23 et `web_search_preview` est legacy : ne pas les utiliser |
| 2026-07-26 | **Réalisation du chantier R1 — trois arbitrages non prévus par le cadrage.** (1) **Effort de raisonnement bloqué à `low`** dans `lib/research/openai-search.ts`, en dur, avec le commentaire qui l'explique : c'est le seul levier qui empêche une requête de se transformer en recherche agentique à plusieurs dizaines de `web_search_call` facturés (10 $ / 1 000 appels d'outil, doc du jour). `search_context_size` ne borne que le contexte injecté, pas le nombre de recherches. (2) **Le nombre de recherches réellement facturées est écrit au journal** (`searches` dans `research_succeeded`, via `countWebSearchCalls`) : `MAX_RESEARCH_PER_DAY` compte des appels `runResearch` — sans ce chiffre, le plafond mentirait sans qu'on le sache. (3) **Un `RESEARCH_PROVIDER` explicite mais sans clé ne tombe pas en repli** sur l'autre fournisseur : `researchProvider()` renvoie `null` (→ `no_key`, dégradation propre). Dépenser chez un fournisseur que l'exploitant n'a pas choisi serait pire qu'une recherche désactivée. Modèle par défaut : celui que la doc désigne pour la Responses API + `web_search`, surchargeable par `RESEARCH_OPENAI_MODEL` sans redéployer | Le cadrage laissait ouverte la question « le nombre de recherches par requête est-il maîtrisable ? ». Réponse : bornable (effort bas) mais **non garanti** par l'API — d'où la mesure au journal plutôt qu'une promesse. Reste à confirmer sur un appel réel (Fathi) : si un appel dépasse ~3 recherches, revoir le plafond en *recherches* et non en *appels* |
| 2026-07-26 | **Navigation à cinq entrées — `Aujourd'hui · Prospects · Campagnes · Mon entreprise · Journal`.** « Plan du mois » (`/plan`) disparaît comme écran : son cap devient un **bandeau sur Aujourd'hui**, en lecture seule, visuellement distinct de la file de validation. « Connecteurs » et « Agent & garde-fous » deviennent des **onglets de « Mon entreprise »** (`?onglet=connecteurs`, `?onglet=agent`). « Contenu » quitte le menu mais **reste une route vivante** (atelier), atteinte par le mouvement « Idées de contenu » du cap et par un bouton sur `/campagnes`. « Journal » reste en premier niveau. Toutes les anciennes URLs redirigent (`/plan` en permanent, `/connecteurs` et `/agent` en temporaire, paramètres de requête conservés) ; les moteurs purs (`lib/plan.ts`, `lib/diagnostic.ts`) ne sont pas modifiés. **Amendement à la maquette V2 de Charly** — décidé par Fathi **sans validation préalable de Charly**, à confirmer avec lui (point 2 de l'ordre du jour de la démo) | **Contexte** : neuf entrées de navigation pour un solopreneur, et **trois écrans où l'agent parle** (propositions sur Aujourd'hui, conseils sur Plan du mois, garde-fous sur Agent) — l'utilisateur ne savait pas où regarder ni où décider. **Décision** : un seul endroit où l'agent s'adresse à l'utilisateur (Aujourd'hui), un seul endroit qui décrit ce que Nepteo sait et a le droit de faire (Mon entreprise), le Journal gardé au premier niveau parce que c'est l'artefact de confiance. **Conséquences** : (1) le cap du mois et la file de validation cohabitent — ils doivent rester **lisiblement distincts**, sinon on recrée la confusion « conseil vs action » qu'on vient de supprimer ; (2) `/contenu` n'a plus qu'**deux points d'entrée** : si les deux disparaissent un jour, l'atelier devient inaccessible sans être supprimé ; (3) tout nouvel écran doit se loger dans l'une des cinq entrées ou justifier la sixième ; (4) si Charly refuse le regroupement, le retour arrière est peu coûteux (la nav est un tableau, les panneaux sont des composants) mais les URLs redirigées auront circulé |
| 2026-07-29 | **Pendant les tests commanditaires, la production reste sans envoi externe. C8 est développé avant C7 ; C7 ne sera activé qu'après décision fournisseur et fermeture des gates rôles/RLS, tenancy, démo, migrations/readiness, suppression-list et claim atomique.** L'outbound utilisera l'outbox PostgreSQL avec un worker borné ; une organisation neuve est utilisée par testeur | L'audit confirme une forte valeur potentielle mais une faible preuve terrain. Le dernier contact améliore la pertinence sans risque externe ; l'envoi est irréversible et ne doit pas précéder les garanties d'autorisation, d'idempotence et d'exploitation |
| 2026-07-29 | **La preuve terrain précède désormais C7.** L'ordre de la phase C devient : recette `0012`–`0019` → C9A de mesure manuelle et Top 5 → play « prospects dormants » → un seul connecteur Gmail ou Microsoft 365 si le manque de contexte franchit le gate et après accord explicite de Fathi → C7 conditionnel → C9B fournisseur. Un fait déclaré n'est jamais présenté comme un statut fournisseur observé | Le benchmark et l'audit croisé convergent vers une boucle étroite « signal → action justifiée → résultat ». Ajouter des tuyaux avant de mesurer l'utilité augmenterait le coût et le risque sans corriger une recommandation faible. Détails et seuils : `docs/projets/roadmap-valeur-commanditaires.md` |
| 2026-07-29 | **La bêta impose une organisation au plus par utilisateur** (`0013`). La migration refuse les doublons sans les modifier et le contexte serveur refuse toute ambiguïté ; un sélecteur d'organisation active est requis avant de retirer cette contrainte | Le produit n'a pas encore d'organisation active explicite. Choisir silencieusement une ligne exposerait à écrire dans le mauvais tenant ; l'invariant mono-org est la solution la plus simple et la plus sûre pour la bêta |
| 2026-07-29 | **`company_memory` est lisible par les membres mais n'est plus directement modifiable avec leur JWT** (`0014`). Les écritures passent par les Server Actions au service role après contrôle du rôle éditeur | La policy initiale `for all` permettait au rôle `lecture` de contourner les gardes de l'application. La frontière d'autorisation doit rester côté serveur |
| 2026-07-29 | **La matrice de rôles devient une frontière applicative et RLS** (`0015`) : admin/marketing/direction peuvent modifier, voir les finances et gérer les campagnes ; lecture voit les finances sans mutation ; commercial ne dispose d'aucune de ces capacités. Comme un texte libre peut recopier notes ou montants, le commercial ne lit ni mémoire, recherche, briefing, action, journal ou outbox ; il conserve seulement les colonnes prospects expurgées et les connecteurs CRM/fichiers | Une UI masquée ne protège pas les lectures JWT ni les chemins service role. Une allowlist de types d'action n'expurge pas leur `payload` ; la frontière simple et fail-closed est adaptée au pilote |
| 2026-07-29 | **Le mode démo est réservé à l'admin d'une organisation de test vide** ; un scénario démo déjà actif peut être remplacé seulement si aucun artefact réel n'est apparu. Les marqueurs explicites, scopes démo et un verrou par organisation bornent chargement, analyse, campagne et nettoyage. Les anciens IDs ne sont fiables qu'avec un marqueur actif | Les données de démonstration ne doivent jamais contaminer un tenant client ni provoquer un nettoyage par ressemblance. Le préflight et le nettoyage sélectif échouent fermés dès qu'une donnée réelle ou ambiguë est détectée |
| 2026-07-29 | **Liveness et readiness sont séparées** : `/api/health` confirme le processus sans base ; `/api/ready` vérifie Supabase et `app_schema_version`, introduit par `0016`. Avant de créer le marqueur, `0016` prouve les invariants critiques de `0012` à `0015`. La révision locale exige désormais `>= 19`, porté par `0019`. Les migrations restent manuelles ; le workflow vérifie le schéma avant toute mutation Azure puis les deux routes après déploiement | Une application qui répond mais dont le schéma est en retard ne doit pas recevoir du trafic. Un numéro écrit à la main ne doit pas non plus certifier des prérequis absents |
| 2026-07-29 | **Le quota quotidien de recherche est un compteur atomique distinct du cache** (`0017`). La réservation verrouille l'organisation et arbitre la pause dans la même transaction : pause gagnante = aucun appel, réservation gagnante = claim payé. Chaque réservation autorisée compte, y compris avec `force` ou si le fournisseur échoue ensuite ; une indisponibilité du compteur bloque l'appel. Seul `research_runs.status = ok` est réutilisable comme cache de réponse | Compter les lignes du cache n'était ni atomique ni fidèle au coût : un appel forcé ou échoué reste facturé. La réservation UTC par organisation ferme les dépassements concurrents et la course entre bouton d'arrêt et appel fournisseur |
| 2026-07-29 | **Les décisions, claims, finalisations et réglages d'exécution sont transactionnels** (`0018`) : transition et journal, vérification pause/autonomie et claim, finalisation et résultat, puis modification d'une garde et événement d'audit sont chacun réalisés dans une seule transaction. Tout claim ou état ambigu exige une reprise explicite | Deux clics ou workers concurrents ne doivent ni dupliquer une décision, ni préparer deux exécutions. Un état métier sans sa trace d'audit violerait le contrat de confiance ; une relance automatique sur état ambigu serait moins sûre qu'un échec fermé |
| 2026-07-29 | **Le durcissement de colonnes est livré par une migration additive** (`0019`) : une base ayant déjà exécuté `0015`/`0016` reçoit à nouveau les policies fail-closed, les JWT perdent `organizations.activity`, `connectors.config` et `connectors.encrypted_credentials`, puis des postconditions contrôlent toute policy parallèle et les privilèges avant de relever la readiness à 19 | Modifier une migration déjà appliquée ne répare pas une base existante. La configuration libre et même un secret chiffré ne doivent pas être exposés au navigateur alors que seules des métadonnées normalisées sont nécessaires |
| 2026-07-30 | **La scorecard intégrée reste strictement locale à une organisation ; les gates du programme sont consolidés manuellement et anonymement hors application.** Aucun total, compteur de testeurs ou signal connecteur ne doit agréger plusieurs tenants. Le « 3 testeurs », les deux pilotes d'un même écosystème et les observations transverses sont rapprochés à partir de fiches pseudonymisées ; l'interface ne montre que les numérateurs, dénominateurs et signaux du tenant courant | Le cloisonnement RLS ne doit pas être contourné au nom de la mesure produit. Un taux local `missing_context` peut orienter le cadrage, mais seule la consolidation programme, complétée par le temps de recherche et l'accord explicite de Fathi, autorise l'étude d'un connecteur |
| 2026-07-30 | **Un environnement de test suit exactement une voie : (A) l'un des trois scénarios Nepteo V2 certifiés, ou (B) des données autorisées apportées par le testeur via connecteur/CSV.** Seul A porte le libellé « données fictives ». Les deux voies ne coexistent jamais : retirer le scénario avant tout import. Les événements fictifs restent exclus des gates terrain. CSV V1 : UTF-8, 900 Ko et 5 000 lignes maximum, mapping non ambigu des six champs utiles, colonnes inconnues ignorées, identifiants stables, remplacement/retrait atomique via `0021`, verrou et journal | Une démonstration sans données ne prouve pas l'agent, mais qualifier d'office toute donnée de test de fictive serait trompeur. L'exclusivité des voies permet d'illustrer le produit, de tester avec les données autorisées du commanditaire et de conserver une frontière nette avec la preuve terrain. La transaction évite qu'une panne expose un mélange de deux imports ou un état non journalisé |
| 2026-07-31 | **La PR #15 constitue l'étape historique de reconstruction de la vitrine sur le schéma minimum 21.** Les migrations `0012` à `0021` restent manuelles ; la voie apportée par le testeur demeure un « environnement de test » et n'est jamais automatiquement qualifiée de « données fictives ». C7 reste fermé et aucun envoi externe n'est activé | La reconstruction a permis les cycles de chargement et d'analyse des trois scénarios et six propositions pour chacun, sans fermer le gate `reset → reseed → préparation → exécution`, le smoke RLS multi-rôles, les OAuth réels ni la preuve terrain commanditaire |
| 2026-07-31 | **La PR #16 est l'étape historique qui livre les trois corrections de recette : provenance du briefing, garde en lecture seule de l'identité/recherche et explication des cohortes.** La révision associée est `nepteo-prod--0000010` | La recette authentifiée a vérifié les libellés, les refus de mutation et les comptages Artisan, puis laissé Northwind actif avec six propositions. Elle a validé ce hotfix, pas le gate complet `reset → reseed → préparation → exécution`. C7 est resté fermé |
| 2026-07-31 | **La PR #17 constituait alors la release courante et livrait une terminologie de surface sans changer la certification technique : « scénario d'exemple Nepteo » et « données d'exemple » pour la voie interne `certified-demo` ; « environnement de test » pour les données saisies ou importées par le testeur, qu'elles soient réelles ou synthétiques.** La révision historique est `nepteo-prod--0000011` | Cette décision remplaçait uniquement le vocabulaire de surface de celle du 2026-07-30 : « fictif » ne doit pas qualifier par déduction les données d'un testeur. Le préflight serveur refuse `unsafe_existing_data` avant toute mutation ; la réponse arme un latch UI fail-closed qui maintient les chargements désactivés jusqu'au rafraîchissement. La recette de production confirmait ces libellés et garde-fous. Le gate complet restait ouvert et C7 fermé |

Jalon historique du 2026-07-30 : les migrations `0012` à `0020` avaient été appliquées manuellement à Supabase (`app_schema_version = 20` à `2026-07-30T06:02:14Z`) ; la PR [#5](https://github.com/Shaaakir281/nepteo/pull/5) avait livré le premier artefact schéma 20, les PR [#7](https://github.com/Shaaakir281/nepteo/pull/7) à [#9](https://github.com/Shaaakir281/nepteo/pull/9) avaient durci les cohortes, puis la PR [#11](https://github.com/Shaaakir281/nepteo/pull/11) avait installé l'affichage prudent de l'environnement de test. Azure servait alors l'image `a2bbc34dcb97ab00951a3efa631c4f7c0a0428ca`, révision `nepteo-prod--0000007`. La migration additive `0021` avait ensuite porté `app_schema_version` à `21` à `2026-07-30T17:40:26Z`.

Étape historique de reconstruction du 2026-07-31 : la PR [#15](https://github.com/Shaaakir281/nepteo/pull/15) a livré le SHA `9889b6e37d6f3856a6ea8721cbd96963d9d8c711` ; sa CI `30614649997` est verte sur le head `6104aae48f016af0203dc8fbe25e30184b0097c9`. La CI `main` `30614730996` et le déploiement `30614818816` sont verts. Azure a créé `nepteo-prod--0000009`, image `nepteoacr27de3b.azurecr.io/nepteo:9889b6e37d6f3856a6ea8721cbd96963d9d8c711`, digest `sha256:7ed581a3ee91181ff4c0928b05d1272ddc7ad65669e774a8031e6e85e4c56ff3`. La reconstruction a retiré exactement 48 prospects, 6 connecteurs et 8 rubriques après sauvegarde (`sha256:ffa9536fadf70d195cebc9b63c4fcfb73e3745ede0e9a20be31348cd6748e07c`), tout en préservant le nom et les membres. Les cycles de chargement et d'analyse des trois scénarios sont verts ; chaque scénario a produit six propositions sans erreur console.

Étape historique du 2026-07-31 : la PR [#16](https://github.com/Shaaakir281/nepteo/pull/16) a livré le SHA `21c90f77af0c877e9c99f60a4997c4dad4b1ba84` ; sa CI `30617946344` est verte sur le head `3a9b228eda3c4480fc9d96f34b51d984dfb28d37`. La CI `main` `30618088771` et le déploiement `30618214620` sont verts. Azure a servi `nepteo-prod--0000010`, image `nepteoacr27de3b.azurecr.io/nepteo:21c90f77af0c877e9c99f60a4997c4dad4b1ba84`, digest `sha256:aff8b036211f25704205a8b9d76237bac3e82a0d2f8a73ab4e08b5319b707ce0`. La recette authentifiée a validé le hotfix de lecture seule et d'explication des cohortes.

Release applicative historique du 2026-07-31 : la PR [#17](https://github.com/Shaaakir281/nepteo/pull/17), head `28781aad52564f02fcee1c0dda4b5ee5291836b8`, a une CI `30620564365` verte. Elle est fusionnée au SHA `704efabd80de434ea2619cd993ae87427c114838` ; la CI `main` `30620691704` et le déploiement `30620812901` sont verts. Azure servait à 100 % `nepteo-prod--0000011`, latest et ready, Healthy/Provisioned/RunningAtMaxScale avec une réplique, image `nepteoacr27de3b.azurecr.io/nepteo:704efabd80de434ea2619cd993ae87427c114838`, digest `sha256:fe6cafbe991c45952262e33be965e4ba09239ff421a86dce80231117a3504425` ; l'application était Succeeded/Running et le FQDN inchangé. Les six contrôles HTTP répondaient 200. La recette authentifiée confirmait « scénario d'exemple » et « données d'exemple », la source du briefing « à partir des données d'exemple du scénario Nepteo », l'identité en lecture seule et la redirection de `/onboarding/identite` vers « Mon entreprise » avec le message de garde ; Northwind restait actif sans mutation et la console était vide. Les 385 tests, le typecheck, le lint et le build de 24 routes étaient verts ; la revue croisée ne relevait aucun P1/P2. Le gate `reset → reseed → préparation → exécution` demeurait ouvert. C7 demeurait fermé et aucun envoi externe n'avait été activé.

Release applicative historique du 2026-08-09 : la PR [#29](https://github.com/Shaaakir281/nepteo/pull/29) est fusionnée au SHA `c5e7148ad62908a52536f6b2b52fd32ed0c357c0` ; sa CI `31332578671` et le déploiement `31332676182` sont verts. Azure servait à 100 % `nepteo-prod--0000022`, image `nepteoacr27de3b.azurecr.io/nepteo:c5e7148ad62908a52536f6b2b52fd32ed0c357c0`, latest et ready. Les routes publiques de santé et readiness répondaient 200. Les 571 tests, le typecheck, le lint et le build de 29 routes sont verts. La recette authentifiée campagne → Story → sélection → approbation confirme le stockage privé, les refus JWT/RLS et l'absence d'effet fournisseur ; les artefacts E2E synthétiques ont été nettoyés. Les gates OAuth réel, concurrence, isolation inter-tenant, pannes Storage/pending et valeur commanditaire restent ouverts ; C7 demeure fermé.

Release applicative historique du 2026-08-10 : la PR [#31](https://github.com/Shaaakir281/nepteo/pull/31) est fusionnée au SHA `7424d2926e6423e1af674c741eb90dc1fcd914a3` ; sa CI `31368969929` et le déploiement `31369161993` sont verts. Azure servait à 100 % `nepteo-prod--0000023`, image `nepteoacr27de3b.azurecr.io/nepteo:7424d2926e6423e1af674c741eb90dc1fcd914a3`, latest et ready. Les 574 tests, le typecheck, le lint et le build de 29 routes sont verts. Connexion et inscription exposent Afficher/Masquer en production ; le schéma reste à 28 et les connecteurs sont inchangés. La source du modèle français a été versionnée par la PR et le modèle hébergé publié séparément ; un nouvel email a ensuite révélé le défaut post-callback corrigé par PR #33.

Release applicative courante du 2026-08-10 : la PR [#33](https://github.com/Shaaakir281/nepteo/pull/33) est fusionnée au SHA `6f54cee5cc7c594956515cea2a94204d5da2a5a2` ; sa CI `31372881341` et le déploiement `31373032666` sont verts. Azure sert à 100 % `nepteo-prod--0000024`, image `nepteoacr27de3b.azurecr.io/nepteo:6f54cee5cc7c594956515cea2a94204d5da2a5a2`, latest et ready, `Succeeded`/`Running`. Les 575 tests, le typecheck, le lint et le build de 29 routes sont verts. Le schéma reste à 28 et les connecteurs sont inchangés. Les quatre variantes négatives de `/auth/confirm` répondent 307 vers le domaine public, sans `0.0.0.0` ; le clic unique d'un email neuf émis après le hotfix, avec jeton non consommé, et le logout mobile authentifié restent à recetter.

Ajouter chaque nouvelle décision ici (format ADR léger : contexte, décision, conséquences).
